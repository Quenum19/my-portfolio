/* =====================================================================
   Interrupteurs de sections du site.
   Passe une valeur à `true` pour réactiver la section correspondante.
   ===================================================================== */

// Section Blog : masquée tant qu'il n'y a pas de vrais articles.
// -> repasser à `true` quand le blog est prêt (nav + pages + sitemap réactivés).
export const BLOG_ENABLED = false;
